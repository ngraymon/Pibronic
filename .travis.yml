language: python
python:
  - 3.6
cache: pip

install:
  - pip install -r requirements.txt
  - pip install pytest pytest-cov
  - pip install codecov

before_script:
  - pytest tests/pimc/test_minimal.py

script:
  - pytest --cov=./

after_success:
  - codecov

notifications:
  email: false
